{{>header}}
{{>navbar}}
<table>
    <thead>
        <tr>
        <th>ID</th>
        <th>Name</th>
        <th>Description</th>
        <th>Target</th>
        </tr>
    </thead>
    <tbody>
        {{#each data}}
        <tr>
            <td>{{id}}</td>
            <td>{{exercise_name}}</td>
            <td>{{exercise_description}}</td>
            <td>{{target}}</td>
        </tr>
        {{/each}}
    </tbody>
</table>

{{>footer}}
